apiVersion: v1
kind: Service
metadata:
  name: traefik
  namespace: test
spec:
  type: LoadBalancer
  selector:
    app: traefik
  ports:
    - port: 80
      targetPort: web
      name: web
    - port: 443
      targetPort: websecure
      name: websecure
